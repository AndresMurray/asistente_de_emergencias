# Runtime del agente de voz. Lo que solo se usa para ingesta o evaluación vive
# en requirements-dev.txt, para que la imagen del agente no lo cargue
# (tamaño de imagen = tiempo de cold start en LiveKit Cloud).
#
# livekit-agents va pineado a propósito: agent.py usa la API nueva
# (AgentServer, inference.*, turn_handling) y los defaults de turn handling
# cambian entre versiones. Sin pin, cualquier rebuild mueve el comportamiento
# de voz y las mediciones dejan de ser comparables.
livekit-agents==1.6.5

# Plugins de proveedores directos (desacoplados del gateway)
livekit-plugins-cartesia~=1.6
livekit-plugins-deepgram~=1.6
livekit-plugins-google~=1.6
livekit-plugins-openai~=1.6

psycopg2-binary==2.9.12
pgvector==0.5.0
aiohttp==3.14.1
httpx>=0.27.0
requests>=2.31.0
python-dotenv==1.2.2