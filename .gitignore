# Python virtual environments
venv/
.venv/

# Python cache
**/__pycache__/
*.pyc
*.pyo
*.pyd

# Environment variables
.env

# Docker env files con credenciales (no commitear passwords reales)
# docker-env/ se incluye en el repo con valores por defecto para la PoC
# Si se usan credenciales reales, descomentar la siguiente línea:
# docker-env/

# Docker appdata (volúmenes persistentes locales)
appdata/ollama/
appdata/owui/
appdata/postgress/
appdata/postgress_vector/
appdata/pipelines/
appdata/rawdata/
!appdata/**/.gitkeep

# Local data (PDFs and processed JSONs)
data/raw/
data/processed/

# Required project exclusion
hoja_ruta_quincena.md
